//! Trajectory schema + JSONL logging shared by every nano-agent recipe.
//!
//! A trajectory is the full record of one repair episode: the initial broken
//! state, every (diagnostic -> proposal -> applied patch -> new diagnostic) step,
//! and the terminal outcome. This is both the research log (spec section 13) and
//! the raw material for SFT (spec section 8).

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use std::fs::{File, OpenOptions};
use std::io::{BufWriter, Write};
use std::path::Path;

/// Terminal outcome of an episode.
#[derive(Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum Outcome {
    Success,
    Escalate,
    NoFix,
    BudgetExhausted,
    Error,
}

/// Which agent produced a proposal in a step (tiny model or frontier teacher).
#[derive(Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum Actor {
    Tiny,
    Frontier,
}

/// The model's structured proposal (spec section 5 output contract).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Proposal {
    pub action: String, // "patch" | "no_fix" | "escalate"
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub patch: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reason: Option<String>,
    #[serde(default)]
    pub confidence: f32,
}

/// One iteration of the closed loop.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Step {
    pub attempt: u32,
    pub actor: Actor,
    /// Primary rustc error code for this step, e.g. "E0308". None if non-error.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub primary_code: Option<String>,
    /// Total error count seen at the start of this step.
    pub error_count: usize,
    /// The context string fed to the model (prompt user turn).
    pub context: String,
    pub proposal: Proposal,
    /// Whether the proposed patch applied cleanly.
    pub patch_applied: bool,
    /// Whether a fuzzy (whitespace-normalized) match was used.
    #[serde(default)]
    pub patch_fuzzy: bool,
    /// Error count after applying the patch (for regression detection).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_count_after: Option<usize>,
    /// True if this patch was reverted by the regression guard.
    #[serde(default)]
    pub reverted: bool,
    /// Wall-clock milliseconds for the model call.
    pub model_latency_ms: u64,
    /// Tokens generated by the model, if reported.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tokens_out: Option<u32>,
}

/// A complete repair episode.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Trajectory {
    pub id: String,
    pub created_at: DateTime<Utc>,
    /// Identifier of the failure case (dataset key / hash).
    pub case_id: String,
    /// Model checkpoint / name used for the tiny actor.
    pub model: String,
    /// Verifier command, e.g. "cargo check".
    pub command: String,
    /// Initial error code distribution (code -> count).
    #[serde(default)]
    pub initial_codes: Vec<String>,
    pub steps: Vec<Step>,
    pub outcome: Outcome,
    /// Total attempts consumed.
    pub attempts: u32,
    /// Whether the frontier fallback was invoked.
    #[serde(default)]
    pub escalated: bool,
    /// End-to-end wall-clock milliseconds.
    pub total_latency_ms: u64,
}

impl Trajectory {
    pub fn new(
        id: impl Into<String>,
        case_id: impl Into<String>,
        model: impl Into<String>,
        command: impl Into<String>,
    ) -> Self {
        Self {
            id: id.into(),
            created_at: Utc::now(),
            case_id: case_id.into(),
            model: model.into(),
            command: command.into(),
            initial_codes: Vec::new(),
            steps: Vec::new(),
            outcome: Outcome::Error,
            attempts: 0,
            escalated: false,
            total_latency_ms: 0,
        }
    }
}

/// Append-only JSONL writer. One trajectory per line.
pub struct TrajectoryLog {
    writer: BufWriter<File>,
}

impl TrajectoryLog {
    pub fn open(path: impl AsRef<Path>) -> std::io::Result<Self> {
        let file = OpenOptions::new().create(true).append(true).open(path)?;
        Ok(Self {
            writer: BufWriter::new(file),
        })
    }

    pub fn append(&mut self, t: &Trajectory) -> std::io::Result<()> {
        let line = serde_json::to_string(t)?;
        self.writer.write_all(line.as_bytes())?;
        self.writer.write_all(b"\n")?;
        self.writer.flush()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn roundtrip_jsonl() {
        let dir = std::env::temp_dir();
        let path = dir.join(format!("traj_test_{}.jsonl", std::process::id()));
        let mut t = Trajectory::new("t1", "case1", "qwen2.5-coder-0.5b", "cargo check");
        t.steps.push(Step {
            attempt: 0,
            actor: Actor::Tiny,
            primary_code: Some("E0308".into()),
            error_count: 1,
            context: "error[E0308]...".into(),
            proposal: Proposal {
                action: "patch".into(),
                patch: Some("<<<<<<< SEARCH...".into()),
                reason: Some("type mismatch".into()),
                confidence: 0.8,
            },
            patch_applied: true,
            patch_fuzzy: false,
            error_count_after: Some(0),
            reverted: false,
            model_latency_ms: 1200,
            tokens_out: Some(42),
        });
        t.outcome = Outcome::Success;
        t.attempts = 1;
        t.total_latency_ms = 1500;

        {
            let mut log = TrajectoryLog::open(&path).unwrap();
            log.append(&t).unwrap();
        }
        let content = std::fs::read_to_string(&path).unwrap();
        let parsed: Trajectory = serde_json::from_str(content.trim()).unwrap();
        assert_eq!(parsed.outcome, Outcome::Success);
        assert_eq!(parsed.steps.len(), 1);
        assert_eq!(parsed.steps[0].primary_code.as_deref(), Some("E0308"));
        std::fs::remove_file(&path).ok();
    }
}
